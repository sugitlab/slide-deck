{
  "name": "slide-deck",
  "version": "1.0.0",
  "description": "Marp slide decks",
  "scripts": {
    "build": "marp --html --output public slides/*.md",
    "preview": "marp -s slides/"
  },
  "dependencies": {
    "@marp-team/marp-cli": "^3.4.0"
  }
}